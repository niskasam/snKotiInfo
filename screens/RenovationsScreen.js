import React from "react";
import { StyleSheet, Text, View } from "react-native";

const RenovationsScreen = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default RenovationsScreen;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});
